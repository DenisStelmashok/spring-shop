spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/springshop
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres

  flyway:
    baseline-on-migrate: true

server:
  hostname: localhost
  port: 8081
mail:
  server:
    host: smtp.gmail.com
    port: 465
    username: d.s.stelmashok@gmail.com
    password: Stel@1983



